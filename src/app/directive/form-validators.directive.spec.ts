import { FormValidatorsDirective } from './form-validators.directive';

describe('FormValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
